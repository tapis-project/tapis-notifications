notifications/release directory
==========================================================================
Files in active use:
==========================================================================
Dockerfile_api - used for building main API service docker image
Dockerfile_dispatch - used for building worker service docker image
docker_build.sh - Build, tag and optionally push docker images for the service
TBD: release_update.sh - Build, tag and push docker images for the release cycle.
TBD: rc.ver - File containing release candidate version as a single integer.
TBD: docker-compose.yml